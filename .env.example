# The app holds no AI keys. Put GEMINI_API_KEY / OPENAI_API_KEY on the hub server
# (see hub/README.md) and build the app with MARKET_HUB_URL pointing to it:
#   MARKET_HUB_URL=https://YOUR-HUB-HOST ./gradlew :app:bundleRelease
